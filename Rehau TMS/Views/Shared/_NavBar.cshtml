<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Rehau TMS", "Index", "Schedule", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                @if (User.Identity.IsAuthenticated)
                {
                    <li>@Html.ActionLink("Kalendarz", "Index", "Schedule")</li>

                    if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
                    {
                        <li>@Html.ActionLink("Artykuły", "Index", "Article")</li>
                        <li>@Html.ActionLink("Narzędzia", "Index", "Tool")</li>
                        <li>@Html.ActionLink("Użytkownicy", "Index", "Account")</li>
                    }
                }
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>


