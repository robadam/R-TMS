@model IEnumerable<Rehau_TMS.ViewModels.UserListViewModel>

@{
    ViewBag.Title = "Użytkownicy";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista użytkowników</h2>
<br />
@Html.ActionLink("Dodaj użytkownika", "Register", "Account", new { @class = "btn btn-default" })
<br />
<br />



@if (!Model.Any())
{
    <p>Brak użytkowniów</p>
}
else
{
    <table class="table table-bordered table-hover">

        <thead>
            <tr>
                <th>Imię i nazwisko</th>
                <th>Login</th>
                <th>Rola</th>
                <th>Status</th>
                <th>Opcje</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.FullName</td>
                    <td>@user.Username</td>
                    <td>@user.Role</td>
                    <td>@(user.IsActive ? "Aktywny" : "Zablokowany")</td>
                    <td>@Html.ActionLink("Edytuj", "Edit", "Account", new { id = user.UserId }, null) | @Html.ActionLink("Zmień hasło", "ChangePassword", "Account", new { Id = user.UserId }, null)</td>
                </tr>
            }

        </tbody>
    </table>
}